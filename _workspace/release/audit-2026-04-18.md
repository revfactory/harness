# Release Audit — 2026-04-18

**Phụ trách:** agent release-engineer
**Repo đối tượng:** `/Users/robin/IdeaProjects/harness` (revfactory/harness)
**Tài liệu chiến lược cấp trên:** `/Users/robin/Downloads/harness-research/revfactory-harness_Jcurve_전략_2026-04-18.md` §3.2 P-04
**Cơ sở:** Báo cáo nghiên cứu §1.5, §4.7 điểm yếu số 4 (lệch tag·version)

---

## 1. Trạng thái hiện tại (Before)

| Nguồn | Phiên bản | Đường dẫn file | Ghi chú |
|------|------|-----------|------|
| Badge README.md | **1.0.1** | `README.md:6` | `version-1.0.1-brightgreen.svg` |
| Badge README_KO.md | **1.0.1** | `README_KO.md:6` | Chuỗi giống bản EN |
| Badge README_JA.md | **1.0.1** | `README_JA.md:6` | Chuỗi giống bản EN |
| plugin.json | **1.2.0** | `.claude-plugin/plugin.json:4` | Nguồn quyền lực thực tế mà Claude Code đọc |
| marketplace.json | **1.1.0** | `.claude-plugin/marketplace.json:14` | `plugins[0].version` |
| CHANGELOG mới nhất | **[1.2.0]** | `CHANGELOG.md:5` | Ngày 2026-04-08 |
| git tag mới nhất | **(không có)** | — | `git tag -l` trả về rỗng, 0 tagged release |
| Commit mới nhất | `3bfc442` | HEAD (main) | `v1.2.0: CLAUDE.md 포인터 정책으로 간소화...` |

### 1.1 Tóm tắt lệch nhau

**Tình trạng lệch nhau 3 chỗ** — vấn đề vệ sinh có thể là trigger từ chối đầu tiên trong quá trình tích lũy tin cậy phê duyệt doanh nghiệp·marketplace.

- Badge README (3 bản) vs plugin.json: **lệch 2 cấp** (1.0.1 → 1.2.0)
- marketplace.json vs plugin.json: **lệch 1 cấp** (1.1.0 → 1.2.0)
- plugin.json vs git tag: **thiếu tag** (đã khai báo 1.2.0 nhưng 0 release artifact)

### 1.2 Mức độ ảnh hưởng của lệch nhau

| Vùng ảnh hưởng | Biểu hiện |
|----------|------|
| Độ tin cậy | Khách xem badge README hiểu nhầm "1.0.1 là mới nhất" → định kiến phiên bản cũ |
| Trải nghiệm cài đặt | Cài qua marketplace đăng ký metadata 1.1.0 nhưng plugin thực tế là 1.2.0 → không theo dõi được cập nhật sau |
| Theo dõi release | `git tag -l` rỗng → `gh release list` rỗng → người dùng không theo dõi được CHANGELOG theo từng version |
| Phê duyệt doanh nghiệp | Không có tagged release thì không lập được CVE·SBOM·audit trail |

---

## 2. Quyết định phiên bản đúng

### Chọn: **v1.2.0**

### Cơ sở

1. **Nguyên tắc ưu tiên plugin.json** — nguồn thực tế mà hệ thống plugin Claude Code đọc nên có quyền lực cao nhất (ưu tiên Step 2 của định nghĩa vai trò)
2. **Khớp với CHANGELOG mới nhất** — `[1.2.0] - 2026-04-08` là entry mới nhất, khớp với plugin.json
3. **Khớp với commit message mới nhất** — `3bfc442 v1.2.0: CLAUDE.md 포인터 정책으로 간소화...` (2 nguồn xác nhận chéo)

→ Đồng bộ nâng badge README (3 bản, đang 1.0.1) và marketplace.json (1.1.0) lên **v1.2.0**

### Phán đoán bổ sung: ghi nhận công việc hiện tại là v1.2.1

- Công việc lần này (đồng bộ version + tuyên bố định vị + CONTRIBUTING + docs/ + phản hồi Issue #3) là cải tiến mức patch/minor sau `v1.2.0`, nên thêm entry **[1.2.1] - 2026-04-18** vào CHANGELOG
- Phán đoán SemVer: không breaking change + thêm tính năng (CONTRIBUTING, docs/) là additive nhưng API skill người dùng không đổi → phán định **patch**
- Khi mơ hồ thì mặc định là minor, nhưng "bề mặt thực thi skill" mà người dùng repo này thấy hoàn toàn không đổi nên giữ patch

---

## 3. Nội dung đồng bộ (After)

| File | Before | After | Cách thay đổi |
|------|--------|-------|----------|
| `README.md:6` | `version-1.0.1-brightgreen` | `version-1.2.0-brightgreen` | Edit (thay chuỗi URL badge) |
| `README_KO.md:6` | `version-1.0.1-brightgreen` | `version-1.2.0-brightgreen` | Edit |
| `README_JA.md:6` | `version-1.0.1-brightgreen` | `version-1.2.0-brightgreen` | Edit |
| `.claude-plugin/marketplace.json:14` | `"version": "1.1.0"` | `"version": "1.2.0"` | Edit |
| `.claude-plugin/plugin.json:4` | `"version": "1.2.0"` | (không đổi) | Đã là phiên bản đúng |
| `CHANGELOG.md` | Mới nhất `[1.2.0]` | Thêm section `[1.2.1] - 2026-04-18` | Edit (chèn ở đầu) |

### 3.1 Danh sách file đã sửa (5 file)

```
 M CHANGELOG.md
 M README.md
 M README_JA.md
 M README_KO.md
 M .claude-plugin/marketplace.json
```

### 3.2 File mới (1 file)

```
?? _workspace/release/audit-2026-04-18.md   (tài liệu audit này)
```

### 3.3 plugin.json **không sửa**

Đã sẵn có phiên bản đúng (`1.2.0`) nên không thay đổi. Cần bump riêng vào thời điểm release phiên bản tiếp theo (1.2.1).

---

## 4. Kế hoạch git tag (chờ phê duyệt thực thi)

**Cấm thực thi** — các lệnh dưới đây chỉ thực thi sau khi được orchestrator chính phê duyệt rõ ràng. Hiện tại chỉ ghi tài liệu.

### 4.1 Tag truy hồi (3 mục)

| Tag | Commit đối tượng | Ngày commit | Cơ sở |
|------|----------|----------|------|
| `v1.0.0` | `dd0d0db` | 2026-03-27 | "마켓플레이스 제출 준비: CHANGELOG 추가, description 영문 병기, 요구사항 정리" — commit đầu tiên có section [1.0.0] trong CHANGELOG |
| `v1.0.1` | `22bdbff` | 2026-03-29 | "README 뱃지 추가 및 plugin.json 버전 1.0.1 동기화" — commit bump thực tế plugin.json 1.0.0→1.0.1 |
| `v1.1.0` | `2d84863` | 2026-04-05 | "v1.1.0: Phase 0 현황 감사, CLAUDE.md 자동 동기화, 운영/유지보수 워크플로우 추가" — commit release cuối cùng của v1.1.0 (commit `8604b11` cùng ngày trước đó là bước trung gian) |

### 4.2 Tag mới (1 mục)

| Tag | Commit đối tượng | Cơ sở |
|------|----------|------|
| `v1.2.0` | `3bfc442` | HEAD hiện tại, "v1.2.0: CLAUDE.md 포인터 정책으로 간소화, 하이브리드 실행 모드 추가" |

### 4.3 Lệnh dự kiến thực thi (chờ phê duyệt)

```bash
cd /Users/robin/IdeaProjects/harness

# Tag truy hồi — chỉ dùng annotated tag (cấm lightweight, §6 định nghĩa vai trò)
git tag -a v1.0.0 dd0d0db -m "v1.0.0: 초기 하네스 메타 스킬 공개

- 6 Phase 워크플로우 기반 하네스 구성
- 6가지 에이전트 아키텍처 패턴
- 에이전트 팀 / 서브 에이전트 실행 모드
- Progressive Disclosure 기반 스킬 생성 가이드"

git tag -a v1.0.1 22bdbff -m "v1.0.1: SKILL.md 중복 제거 및 plugin.json 버전 동기화

- SKILL.md ↔ references 간 중복 내용 제거 (330줄 → 285줄)
- Phase 2-1, 2-3, 3, 5-2 references 포인터 전환"

git tag -a v1.1.0 2d84863 -m "v1.1.0: Phase 0 현황 감사, CLAUDE.md 동기화, 하네스 진화 메커니즘

- Phase 0: 현황 감사 및 3분기 라우팅 (신규/확장/유지보수)
- Phase 5-4: CLAUDE.md 하네스 컨텍스트 등록
- Phase 7: 하네스 진화 메커니즘 (피드백→반영→변경 이력)
- 오케스트레이터 후속 작업 지원"

git tag -a v1.2.0 3bfc442 -m "v1.2.0: CLAUDE.md 포인터 정책, 하이브리드 실행 모드

- CLAUDE.md 등록 정책 간소화 (컨텍스트 → 포인터)
- Phase 2-1 하이브리드 실행 모드 (팀 + 서브 조합)
- Phase 5-0 하이브리드 오케스트레이터 패턴
- Phase 5-1 반환값 기반 데이터 전달 (서브 모드)"

# Xác nhận
git tag -l --sort=-v:refname
git show v1.2.0 --stat | head -20
```

### 4.4 Push remote (chờ phê duyệt kép)

```bash
# ⚠️ Chỉ thực thi sau khi đã kiểm chứng 4 tag cục bộ trên và được orchestrator chính phê duyệt lần 2
git push origin v1.0.0 v1.0.1 v1.1.0 v1.2.0
```

**Nguyên tắc:** không dùng `git push --tags` (push toàn bộ). Push riêng từng tag trong 4 tag để tránh sự cố.

---

## 5. Nháp GitHub Release (chờ phê duyệt)

**Cấm thực thi** — `gh release create` kể cả `draft` đều chờ phê duyệt. Dưới đây chỉ đề xuất nội dung lệnh.

### 5.1 v1.2.0 (phiên bản đúng hiện tại)

```bash
cd /Users/robin/IdeaProjects/harness

gh release create v1.2.0 \
  --title "v1.2.0 — CLAUDE.md 포인터 정책 & 하이브리드 실행 모드" \
  --notes "$(cat <<'EOF'
> ⚠️ **Experimental**: Claude Code 플러그인 시스템은 현재 Experimental 단계입니다. 프로덕션 도입 시 pin-to-tag 전략을 권장합니다.

## Changed

- **CLAUDE.md 등록 정책 간소화 (중복 제거)** — Phase 5-4 "컨텍스트 등록"을 "포인터 등록"으로 전환. 에이전트 목록·스킬 목록·디렉토리 구조·실행 규칙 상세를 CLAUDE.md에서 제거하고 **트리거 규칙 + 변경 이력**만 남김
- **Phase 3/4 임시 동기화 단계 삭제** — CLAUDE.md 동기화 부담 축소
- **핵심 원칙 3번 재정의** — "컨텍스트 등록" → "포인터 등록"

## Added

- **Phase 2-1: 하이브리드 실행 모드** — 에이전트 팀 / 서브 에이전트 외 하이브리드 패턴 추가
- **Phase 2-1 실행 모드 비교표** — 팀/서브/하이브리드 3종 의사결정 순서
- **Phase 5-0 하이브리드 오케스트레이터 패턴**
- **Phase 5-1 반환값 기반 데이터 전달** (서브 에이전트 모드 전용)

## Migration

이전 버전(v1.1.0)에서 CLAUDE.md 섹션 "하네스 컨텍스트"가 비대해진 사용자는 `docs/migration-1.2.md` (준비 중)를 참고하여 포인터 블록으로 전환하세요.

## Full Changelog

[CHANGELOG.md §1.2.0](https://github.com/revfactory/harness/blob/main/CHANGELOG.md)
EOF
)" \
  --draft
```

### 5.2 v1.0.0 / v1.0.1 / v1.1.0 (release truy hồi)

Sau khi tạo tag, trích nội dung `--notes` từ section tương ứng trong CHANGELOG. Bỏ qua đề xuất lệnh trong audit này (xử lý ở batch thứ 2 sau khi v1.2.0 được phê duyệt).

### 5.3 Checklist phê duyệt thực thi

Cần kiểm tra trước khi orchestrator chính phê duyệt chạy `gh release create`:

- [ ] 4 tag cục bộ đã gắn đúng SHA chưa (kiểm chứng bằng `git show <tag>`)
- [ ] Sau khi `git push origin v*` hoàn tất, đã xác nhận tag hiện trên GitHub chưa
- [ ] Đầu release note đã có cảnh báo Experimental chưa (Step 6 định nghĩa vai trò)
- [ ] Đã xác nhận content-creator có thể đính ảnh banner vào release chưa

---

## Phụ lục A. Checklist cho lần audit tiếp theo

- [ ] Số commit mới sau audit lần này
- [ ] Version plugin.json có thay đổi trong khoảng đó không
- [ ] Có thêm entry mới vào CHANGELOG không
- [ ] Khoảng lệch giữa version tag cuối và plugin.json
- [ ] Trạng thái đồng bộ của 3 badge README

## Phụ lục B. Liên kết tài liệu cấp trên

- Báo cáo chiến lược §3.2 P-04 "Tính nhất quán version·release" — trigger trực tiếp của công việc này
- Báo cáo chiến lược §4.7 "Đường phê duyệt doanh nghiệp" — yêu cầu tagged release
- Báo cáo nghiên cứu §1.5 "Chỉ số định lượng repo" — nguyên nhân tình trạng 0 release
- Báo cáo nghiên cứu §4.7 điểm yếu 4 "Lệch tag·version" — đối tượng giải quyết của công việc này
